import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: "Briyith's Closet",
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
